export default function Header() {
  return (
    <header className="bg-white shadow px-6 py-4 flex items-center justify-between">
      <h1 className="text-xl font-bold text-gray-900">Satom IT Learning</h1>
      <nav>
        {/* Ici on pourra ajouter user menu, logout, notifications icon */}
      </nav>
    </header>
  );
}
